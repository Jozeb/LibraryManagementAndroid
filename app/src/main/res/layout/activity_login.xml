<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center_horizontal"
    android:layout_gravity="center"
    android:background="@color/colorPrimaryDark2"
    tools:context="com.example.jazibhassan.thelibrary.LoginActivity">
<!-- Inam ny suggest kia
Forgot password bhe daldo -->

<TextView
    android:id="@+id/title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="LIBRARY"
    android:gravity="center"
    android:textSize="45sp"

    android:textStyle="bold"
    android:textColor="@color/colorText"
    android:layout_margin="40dp"/>

<LinearLayout
    android:layout_below="@+id/title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minWidth="300dp"
    android:orientation="vertical"
    android:padding="35dp"
    >

    <EditText
        android:layout_margin="5dp"
        android:background="@drawable/editfield1"
        android:gravity="center"
        android:id="@+id/login_id_t"
        android:layout_width="match_parent"
        android:layout_height="@dimen/login_t"
        android:hint="ID: KXX-XXXX"
        android:textSize="20dp"

        android:maxLength="7"
        android:maxLines="1"
        android:textColor="@color/colorPrimaryText"/>

    <EditText

        android:layout_margin="5dp"
        android:background="@drawable/editfield1"
        android:gravity="center"
        android:id="@+id/login_pass_t"
        android:layout_width="match_parent"
        android:layout_height="@dimen/login_t"
        android:maxLines="1"
        android:inputType="textPassword"
        android:hint="Enter password"
        android:textSize="20dp"
        android:textColor="@color/colorPrimaryText"/>

    <Button
        android:layout_margin="5dp"
        android:layout_gravity="right"
        android:layout_width="match_parent"
        android:layout_height="@dimen/login_t"
        android:text="Login"
        android:id="@+id/login"
        android:background="@drawable/button1_selector"
        android:textColor="@color/colorText"

        />

    <Button
        android:layout_margin="5dp"
        android:layout_gravity="right"
        android:layout_width="match_parent"
        android:layout_height="@dimen/login_t"
        android:text="Signup"
        android:id="@+id/signup"
        android:background="@drawable/button2_selector"
        android:textColor="@color/colorText"

        />

</LinearLayout>







</RelativeLayout>
